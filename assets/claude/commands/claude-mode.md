---
description: Show how to switch Claude modes (subscription/api/local)
---
Run `claude-mode` in Bash and show the output, then summarize the key steps briefly.

If `claude-mode` is not found, provide this fallback:
1. `claude-mode subscription`
2. `claude-mode api-key set <your_anthropic_api_key>`
3. `claude-mode api`
4. `claude-mode local`
5. `source ~/.config/claude-code/mode.env`
